#VIM config 

##Installation

Grab the plugin submodules: 
`` git submodule init && git submodule update

Symlink vimrc: 
`` ln -s ~/.vim/.vimrc ~/.vimrc

Global gitignore:
`` ln -s ~/.vim/.gitignore_global ~/.gitignore
`` git config --global core.excludesfile ~/.gitignore
